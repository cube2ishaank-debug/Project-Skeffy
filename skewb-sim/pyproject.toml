[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "skewbsim"
version = "1.0.0"
description = "A Skewb simulator in Python."
readme = "README.md"
requires-python = ">=3.8"
authors = [
    { name = "Ishaank Bansal" }
]